server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/animeapp
    username: root
    password:
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  
  application: 
    name: AnimeApp